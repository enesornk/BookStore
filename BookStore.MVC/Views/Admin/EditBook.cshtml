@model BookStore.MVC.Models.BookViewModel
@{
    ViewData["Title"] = "Kitap Düzenle";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <h1>@ViewBag.Title</h1>
        </div>
    </div>

    <div class="row">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body">
                    <form asp-action="EditBook" asp-route-id="@Model.Id" method="post" enctype="multipart/form-data">
                        <input type="hidden" asp-for="Id" />
                        <input type="hidden" asp-for="CreatedDate" />
                        
                        <div class="mb-3">
                            <label asp-for="Title" class="form-label">Kitap Başlığı</label>
                            <input asp-for="Title" class="form-control" required />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Author" class="form-label">Yazar</label>
                            <input asp-for="Author" class="form-control" required />
                            <span asp-validation-for="Author" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Description" class="form-label">Açıklama</label>
                            <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Price" class="form-label">Fiyat</label>
                            <input asp-for="Price" type="number" step="0.01" class="form-control" required />
                            <span asp-validation-for="Price" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="CategoryId" class="form-label">Kategori</label>
                            <select asp-for="CategoryId" asp-items="Model.Categories" class="form-select" required>
                                <option value="">Kategori Seçin</option>
                            </select>
                            <span asp-validation-for="CategoryId" class="text-danger"></span>
                            <small class="form-text text-muted">Mevcut kategori: @(Model.CurrentCategoryName ?? "Kategori Yok") | Lütfen bir kategori seçin.</small>
                        </div>

                        <div class="mb-3">
                            <label asp-for="BookImage" class="form-label">Yeni Kitap Resmi</label>
                            <input asp-for="BookImage" type="file" class="form-control" accept="image/*" />
                            <small class="form-text text-muted">PNG, JPG, JPEG dosyaları kabul edilir. Maksimum 5MB.</small>
                        </div>

                        <div class="mb-3">
                            <label asp-for="ImageUrl" class="form-label">Resim URL (Alternatif)</label>
                            <input asp-for="ImageUrl" class="form-control" placeholder="https://example.com/image.jpg" />
                            <small class="form-text text-muted">Dosya yüklemek yerine URL de kullanabilirsiniz.</small>
                            <span asp-validation-for="ImageUrl" class="text-danger"></span>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save"></i> Güncelle
                            </button>
                            <a href="@Url.Action("Books", "Admin")" class="btn btn-secondary">
                                <i class="fas fa-arrow-left"></i> Geri
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card">
                <div class="card-header">
                    <h5>Kitap Bilgileri</h5>
                </div>
                <div class="card-body">
                    <p><strong>ID:</strong> @Model.Id</p>
                    <p><strong>Oluşturulma Tarihi:</strong> @Model.CreatedDate.ToString("dd.MM.yyyy HH:mm")</p>
                    
                    @if (!string.IsNullOrEmpty(Model.ImageUrl))
                    {
                        <div class="mt-3">
                            <p><strong>Mevcut Resim:</strong></p>
                            <img src="@Model.ImageUrl" class="img-fluid rounded" alt="@Model.Title" style="max-height: 200px; object-fit: cover;">
                            <small class="form-text text-muted">Yeni resim yüklerseniz bu resim değiştirilecektir.</small>
                        </div>
                    }
                    else
                    {
                        <div class="mt-3">
                            <p><strong>Mevcut Resim:</strong></p>
                            <div class="bg-light d-flex align-items-center justify-content-center rounded" style="height: 200px;">
                                <span class="text-muted">📚 Resim Yok</span>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div> 